export class CloseBracketOperation {

    constructor() {
        this.name = 'closeBracket';
        this.symbol = ')';
        this.operator = ')';
    }

    operate(calcData) {

        return calcData;
    }
}